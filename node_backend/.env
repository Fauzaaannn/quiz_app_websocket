KEYCLOAK_BASE_URL=http://localhost:8080
KEYCLOAK_REALM=learning-realm
KEYCLOAK_CLIENT_ID=test-node-server
KEYCLOAK_CLIENT_SECRET=Yb1n2P3nwVe2S6X4MKUuughUD3Y1pRUf
REDIRECT_URI=http://localhost:3000/callback
# REDIRECT_URI=http://localhost:3000/auth/*

# For mobile apps
FRONTEND_REDIRECT_URI=com.yourapp://callback

# For web apps
# FRONTEND_REDIRECT_URI=http://localhost:3000/success

# Where to go after Keycloak end-session completes (must be whitelisted in client > Valid post logout redirect URIs)
POST_LOGOUT_REDIRECT_URI=http://localhost:3000/logout-complete

# Optional: where to deep link after /logout-complete. If omitted and using a custom scheme
# in FRONTEND_REDIRECT_URI, the server will auto-derive scheme://login
# FRONTEND_LOGOUT_REDIRECT_URI=com.yourapp://login

